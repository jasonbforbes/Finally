# Review

Unable to review changes since the last commit.

The shell command runner failed before PowerShell started with:

```text
windows sandbox: spawn setup refresh
```

Commands attempted:

- `git status --short`
- `git diff --stat HEAD`
- `git diff --name-status HEAD`
- `Get-Location`

Because the repository diff could not be inspected, no code findings can be reported yet. Re-run the review after command execution is available.
